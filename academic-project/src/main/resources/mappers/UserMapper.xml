<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.hyucs.academic.mapper.UserMapper">
	<insert id="insert">
		INSERT INTO tbl_user(uid, upw, uname, email)
		VALUES(#{uid}, #{upw}, #{uname}, #{email})
	</insert>
	
	<select id="login" resultType="UserVO">
		SELECT *
		FROM tbl_user
		WHERE uid=#{uid}
	</select>
	
	<select id="loginCheck" resultType="UserVO">
		SELECT *
		FROM tbl_user
		WHERE uid=#{uid}
	</select>
	
	<select id="countById" resultType="int">
		SELECT COUNT(*)
		FROM tbl_user
		WHERE uid=#{uid}
	</select>
	
	<update id="keepLogin">
		UPDATE tbl_user
		SET sessionkey=#{sessionId}, sessionlimit=#{next}
		WHERE uid=#{uid}
	</update>
	
	<select id="checkUserWithSessionKey" resultType="UserVO">
		SELECT *
		FROM tbl_user
		WHERE sessionkey=#{value} AND sessionlimit > now()
	</select>
</mapper>